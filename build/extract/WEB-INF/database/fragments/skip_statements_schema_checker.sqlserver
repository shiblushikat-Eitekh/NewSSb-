-- You can define here all the statements that should be excluded by the schema checker,
-- these statements are not going to be included in the upgrade file.

-- we are recreating the table so these don't need to be included
alter table identityiq.spt_post_commit_notification_object add consumer nvarchar(256);
alter table identityiq.spt_post_commit_notification_object add created numeric(19,0);
alter table identityiq.spt_post_commit_notification_object add committed_object_string nvarchar(max);

-- Setting not null on exsiting column is not supported by all DBMS
alter table identityiq.spt_bundle add iiq_elevated_access tinyint not null;
alter table identityiq.spt_managed_attribute add iiq_elevated_access tinyint not null; 
